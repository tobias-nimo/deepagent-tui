# LangGraph server URL
# LANGGRAPH_URL=http://localhost:2024

# Graph ID (default: auto-discover from server)
# GRAPH_ID=...

# LangSmith API key (optional, for authenticated LangGraph Cloud connections)
# LANGSMITH_API_KEY=...

# UI theme (default: default). One of:
#   default, aesthetic, vintage, monochrome, terminal, sunset, ocean, neon
# DEEPAGENT_THEME=ocean
